name: Demo Workflow 
on: workflow_dispatch
jobs:
  print_demo:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello GH
      - name: Print a few lines0909
        run: |
          echo First line!
          echo Second line!
